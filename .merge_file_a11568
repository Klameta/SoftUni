function solve(numberArr) {
    for (const currNum of numberArr) {
        const currStr = currNum.toString().split("");
        for (let i = 0; i < currStr.length / 2; i++) {
            let left = currStr.pop

        }
    }
}